openapi: 3.0.4
info:
  title: Backend API OCaml Dream
  description: 
  version: 1.0

# TODO: finish responses from every route and add correct shcemas

paths:
  /users:
    get:
      summary: Returns a list of users.
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
    post:
      summary: Creates a user.
      description: 
      responses:
        "200": 
          description: 
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /users/{id}:
    get:
      summary: Returns a user.
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
    put:
      summary: Update a user.
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
    delete:
      summary: Deletes a user.
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /auth/login:
    post:
      summary: Authenticate a user.
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /auth/register:
    post:
      summary: Creates a user.
      description: Admin only
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /contests:
    get:
      summary: Returns a list of contests.
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
    post:
      summary: Create a contests.
      description: Admin only
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /contests/{id}:
    get:
      summary: Returns a contests detail.
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
    put:
      summary: Update a contests.
      description: Admin only
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
    delete:
      summary: Delete a contests.
      description: Admin only
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /contests/{contestsId}/problems:
    get:
      summary: List contest problems.
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
    post:
      summary: Create problem.
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /problems/{id}:
    get:
      summary: Get problem details.
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
    put:
      summary: Update problem.
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
    delete:
      summary: Delete problem.
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /problems/{id}/testcases:
    get:
      summary: Upload test case.
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /problems/{id}/testcases:
    get:
      summary: List test cases (admin/judge)
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /submissions:
    post:      
      summary: Submit solution
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /submissions/{id}:
    get:
      summary: Get submission result
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string 
  /contests/{contestId}/submissions:
    get:
      summary: List contest submissions (filtered by user/problem/status)
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string       
  /judge/next:
    post:
      summary: Judge worker fetches next submission
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string    
  /judge/{submissionId}/result:
    post:
      summary: Judge posts result
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string    
  /contests/{id}/scoreboard:
    get:
      summary: 
      description: 
      responses:
        "200": 
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string